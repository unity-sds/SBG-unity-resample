# DO NOT DELETE
# THIS CONFIG IS AUTO-GENERATED BY ADE UI
algorithm_name: sister-resample
algorithm_version: 2.0.1
repository_url: https://github.com/EnSpec/sister-resample.git
docker_container_url: mas.dit.maap-project.org/root/maap-workspaces/base_images/vanilla:dit

# fill out these fields
# explain what this algorithm does
algorithm_description: Spectrally resample reflectance data to SBG-like resolution (10nm).
# path to the build script for installing and building the algorithm in the docker image
build_command: sister-resample/install.sh
# path to the wrapper script for running the algorithm
run_command: sister-resample/pge_run.sh
# set a storage value in GB or MB or KB, e.g. "100GB", "20MB", "10KB"
disk_space: 50GB
inputs:

  file:
    - name: reflectance_dataset
      description:
      required: True

    - name: uncertainty_dataset
      description:
      required: True

  config:
    - name: crid
      data_type:
      default: '000'
      description:
